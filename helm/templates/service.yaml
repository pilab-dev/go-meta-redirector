apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-meta-redirector.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "go-meta-redirector.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: {{ .Values.service.protocol }}
      name: http
  selector:
    {{- include "go-meta-redirector.selectorLabels" . | nindent 4 }}
